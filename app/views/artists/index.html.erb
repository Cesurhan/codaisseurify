<h1>All Artists:</h1>
<hr>

<% @artists.each do |artist| %>
<% @photos.each do |photo| %>
<%= image_tag photo.image if photo.artist_id == artist.id%>
<%end%>
<h3><%= link_to "#{artist.first_name} #{artist.last_name}", artist %></h3>
<%end%>
