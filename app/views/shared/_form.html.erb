<div class="col-md-6">
    <%= form_for @song do |f|%>
    <div class="form-group">
      <%= f.label "Add New Song:"%>
      <%= f.text_field :title,  :class => 'form-control', :placeholder => 'Title',  autocomplete: "off" %>
    </div>
      <!-- <div class="form_group">
        <%= f.label :album %>
        <%= f.text_field :album, :class => 'form-control', :placeholder => 'Album',  autocomplete: "off" %>
      </div>
      <div class="form_group">
        <%= f.label :release_date %>
        <%= f.date_field :release_date, :class => 'form-control', :placeholder => 'Release Date',  autocomplete: "off" %>
      </div>
      <div class="form_group">
        <%= f.label :award %>
        <%= f.text_field :award, :class => 'form-control', :placeholder => 'Award',  autocomplete: "off" %>
      </div> -->
      <div class="form_group">
      <%= f.hidden_field :artist_id, :class => 'form-control', :value => "#{@artist.id}",  autocomplete: "off"%>
    </div>
      <hr>

      <div class="actions">
        <%= link_to "Back", artists_path, class: "btn btn-default" %>
        <%= f.submit (@song.new_record? ? "Add" : "Update"), :class => "btn btn-primary" %>
      </div>
      <% end %>
</div>
